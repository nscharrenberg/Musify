@model Musify_Web.Models.ViewModel.ArtistGenreViewModel

<br />
<h1>Are you sure you want to delete <span class="text-primary">@Model.Genre.Name</span> genre from <span class="text-primary">@Model.Artist.Name</span>?</h1>
<hr />
    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        <div class="row">
            <div class="col-lg-12">
                <div class="form-group">
                    
                </div>
            </div>
            <div class="col-lg-12">
                <div class="form-group">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-danger btn-lg btn-block">Delete @Model.Genre.Name from @Model.Artist.Name</button>
                </div>
            </div>
            
        </div>
            
    }


@model Musify_Web.Models.Artist

@{
    ViewBag.Title = "Albums";
    Layout = null;
}

<h2>Albums</h2>


<div class="row">
    <div class="col-lg-12">
        <div class="form-group">
            <a class="btn btn-primary btn-block btn-lg" href="@Url.Action("AddGenres", "Artists")">Add new Album to @Model.Name</a>
        </div>
    </div>
    <div class="col-lg-12">
        <div class="form-group">
            <input type="text" id="searchBar" onkeyup="searchItem()" placeholder="Search for the Artists Genre..">
        </div>
    </div>
    <div class="col-lg-12">
        <table class="table table-striped table-hover " id="indexTbl">
            <thead>
            <tr>
                <th>Name</th>
                <th>Description</th>
                <th>Image</th>

            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model.Albums)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Name)</td>
                    <td>@Html.DisplayFor(modelItem => item.ReleaseDate)</td>
                    <td>@Html.DisplayFor(modelItem => item.ImageBigUrl)</td>
                    <td><a class="btn btn-danger" href="@Url.Action("DeleteGenreFromArtist", "Artists", new { artistId = Model.Id, genreId = item.Id })">Delete</a></td>
                </tr>
            }

            </tbody>
        </table>
    </div>
</div>


<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>


